Put your Google OAuth files here (both are gitignored):

- `client_secret.json` — downloaded from Google Cloud Console (see project README.md, step 2)
- `token.json` — created automatically after your first successful sign-in
