Put your Gmail OAuth files here (both are gitignored):

- `client_secret.json` — downloaded from Google Cloud Console (Desktop app OAuth client, `gmail.modify` scope)
- `token.json` — created automatically after your first successful sign-in
